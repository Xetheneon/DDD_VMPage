name: Push To Windows VM

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      - name: Copy to host directory
        run: powershell Copy-Item -Path G:\actions-runner\_diag\Runner_20240209-144333-utc\* -Destination C:\inetpub\wwwroot -force
